export default function Band() {
  return (
    <section className="band">
      <div className="wrap" style={{ display: 'flex', justifyContent: 'center', textAlign: 'center' }}>
        <h2 style={{ fontSize: 'clamp(1.5rem,3.4vw,2.5rem)', fontWeight: 900, maxWidth: '22ch' }}>
          From survey to safe handover — one specialist partner for the ground.
        </h2>
      </div>
    </section>
  )
}
